fastapi==0.115.0
uvicorn[standard]==0.30.0
sqlalchemy==2.0.35
psycopg[binary]>=3.2,<4  # PostgreSQL driver — required when DATABASE_URL is set (Neon/Vercel)
python-dotenv==1.0.1
slowapi==0.1.9
user-agents==2.2.0
qrcode[pil]==7.4.2
PyJWT[crypto]>=2.8,<3  # runtime dep — Clerk JWT verification (RS256 needs cryptography)
pydantic>=2.9,<3
sentry-sdk>=2.69.2,<3.0

# Testing
pytest
httpx
